#compilingToy.

= Compiling MinD Toy =

==Dependencies==

For compiling Toy version, you will need the following packages installed before executing _configure_ command:
  * pkg-config
  * build-essential
  * libz-dev
  * libpcre3-dev
  * libclamav-dev

==Download==

Get source code from Google Code repository:
{{{
svn checkout http://mindwebfilter.googlecode.com/svn/trunk/ mindwebfilter-read-only
}}}

==Configuring==
For using MinD with Clam Antivirus protection just execute:
{{{
./configure --enable-clamd=yes --enable-icap=yes --enable-commandline=yes \
--enable-trickledm=yes --enable-ntlm=yes CXX="g++" CXXFLAGS="-O3" \
LDFLAGS="-Wl,-Bsymbolic-functions" CPPFLAGS="" CC="cc" "CFLAGS=-O3" \
--prefix="" --with-proxyuser=proxy --with-proxygroup=proxy
}}}

==Compiling==
Once configuration has finished just type:
{{{
make
}}}
And wait patiently...

==Installing==
After compiling just type:
{{{
sudo make install
}}}